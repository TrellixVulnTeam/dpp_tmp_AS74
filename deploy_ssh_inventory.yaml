all:
  vars:
    target_usr: "dpp"
    target_pub_key: "/home/scm/.ssh/id_rsa.pub"
  
  hosts:
    mgmt1:
      ansible_host: mgmt1
      ansible_connection: ssh
      ansible_user: scm
      #ansible_ssh_private_key_file: /home/scm/.ssh/id_rsa.pub 
      #ansible_ssh_pass: "APASSWORD!" # -> please avoid hardcoded passwords and use --ask-become-pass instead

    mgmt2:
      ansible_host: mgmt2
      ansible_connection: ssh
      ansible_user: scm
      #ansible_ssh_private_key_file: /home/scm/.ssh/id_rsa.pub 
      #ansible_ssh_pass: "APASSWORD!" # -> please avoid hardcoded passwords and use --ask-become-pass instead
